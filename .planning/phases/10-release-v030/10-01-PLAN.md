---
phase: 10-release-v030
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/jaeger_mcp/__init__.py
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: [REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "pyproject.toml version field reads 0.3.0"
    - "__init__.py __version__ reads 0.3.0"
    - "CHANGELOG.md has a v0.3.0 section documenting jaeger_compare_traces and jaeger_span_statistics"
    - "README.md tool table lists 7 tools including the two new trace analysis tools"
    - "README.md has a trace analysis section with usage examples for compare_traces and span_statistics"
    - "README.md read-only line says 7 tools"
    - "All 237+ tests pass with coverage >= 90%"
  artifacts:
    - path: "pyproject.toml"
      provides: "Version bump to 0.3.0"
      contains: 'version = "0.3.0"'
    - path: "src/jaeger_mcp/__init__.py"
      provides: "Python-level version 0.3.0"
      contains: '__version__ = "0.3.0"'
    - path: "CHANGELOG.md"
      provides: "v0.3.0 release notes"
      contains: "## [0.3.0]"
    - path: "README.md"
      provides: "Updated tool docs with 7 tools"
      contains: "jaeger_compare_traces"
  key_links:
    - from: "pyproject.toml"
      to: "src/jaeger_mcp/__init__.py"
      via: "version string must match"
      pattern: "0\\.3\\.0"
    - from: "CHANGELOG.md"
      to: "README.md"
      via: "both document the same two new tools"
      pattern: "jaeger_compare_traces|jaeger_span_statistics"
---

<objective>
Release v0.3.0 — bump version, write changelog, update README for the two new trace analysis tools.

Purpose: Prepare the package for publish with accurate documentation reflecting all 7 MCP tools.
Output: Updated pyproject.toml, __init__.py, CHANGELOG.md, README.md — ready to tag and publish.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-release-v030/10-CONTEXT.md
@pyproject.toml
@src/jaeger_mcp/__init__.py
@CHANGELOG.md
@README.md

<interfaces>
<!-- Version is defined in two places — both must be updated: -->

From pyproject.toml (line 7):
```toml
version = "0.2.0"
```

From src/jaeger_mcp/__init__.py (line 3):
```python
__version__ = "0.2.0"
```

<!-- The 7 registered MCP tools (from tools.py): -->
1. jaeger_list_services     — GET /api/services
2. jaeger_list_operations   — GET /api/services/{service}/operations
3. jaeger_search_traces     — GET /api/traces
4. jaeger_get_trace         — GET /api/traces/{traceID}
5. jaeger_get_dependencies  — GET /api/dependencies
6. jaeger_compare_traces    — GET /api/traces/{traceID} (×2, then diff)
7. jaeger_span_statistics   — GET /api/traces (aggregates across N traces)

<!-- Facade methods (from facade.py): -->
compare_traces(trace_id_a, trace_id_b) -> TraceComparison
span_statistics(service, *, operation=None, limit=20) -> SpanStatisticsResult

<!-- Domain types exported from __init__.py: -->
JaegerClient, OperationStatResult, ServiceDep, Span, SpanChange,
SpanIdentity, SpanStatisticsResult, Trace, TraceComparison, TraceSummary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump to 0.3.0</name>
  <files>pyproject.toml, src/jaeger_mcp/__init__.py</files>
  <read_first>pyproject.toml, src/jaeger_mcp/__init__.py</read_first>
  <action>
Update the version string in both files from "0.2.0" to "0.3.0":

1. In `pyproject.toml` line 7: change `version = "0.2.0"` to `version = "0.3.0"`
2. In `src/jaeger_mcp/__init__.py` line 3: change `__version__ = "0.2.0"` to `__version__ = "0.3.0"`

Also update the `pyproject.toml` description (line 8) to mention trace analysis:
- Current: `"MCP server for Jaeger distributed tracing — search traces, inspect spans, map service dependencies."`
- New: `"MCP server for Jaeger distributed tracing — search traces, inspect spans, compare traces, compute span statistics, map service dependencies."`

These are the ONLY two version sources in the project; no other files contain a version string.
  </action>
  <verify>
    <automated>grep -c '"0.3.0"' pyproject.toml src/jaeger_mcp/__init__.py | grep -v ':0$' | wc -l | xargs test 2 -eq</automated>
  </verify>
  <acceptance_criteria>
- pyproject.toml `version` field is exactly `"0.3.0"`
- __init__.py `__version__` is exactly `"0.3.0"`
- pyproject.toml description mentions trace comparison and span statistics
- No other file references version 0.2.0
  </acceptance_criteria>
  <done>Both version fields read 0.3.0, description updated</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG and README update</name>
  <files>CHANGELOG.md, README.md</files>
  <read_first>CHANGELOG.md, README.md</read_first>
  <action>
**CHANGELOG.md** — Insert a new `## [0.3.0]` section ABOVE the existing `## [0.2.0]` entry. Follow the existing Keep a Changelog format exactly. Content:

```markdown
## [0.3.0] — 2026-06-16

### Added

- **`jaeger_compare_traces` tool** — structural diff between two traces
  - Matches spans by `(operationName, serviceName, parentOperation)` tuple — not span ID
  - Reports added spans, removed spans, and changed spans with duration deltas and tag differences
  - Structured output: `CompareTracesOutput` with `added_spans`, `removed_spans`, `changed_spans`, `unchanged_count`
- **`jaeger_span_statistics` tool** — per-operation latency percentiles and error rates
  - Aggregates across N traces for a service (configurable limit, default 20, max 100)
  - Per-operation stats: count, p50/p95/p99 duration (μs), error count, error rate
  - Structured output: `SpanStatisticsOutput` with per-operation `OperationStats` list
- **`JaegerClient.compare_traces()`** — facade method for in-process trace comparison
- **`JaegerClient.span_statistics()`** — facade method for in-process span stats aggregation
- New domain types: `TraceComparison`, `SpanIdentity`, `SpanChange`, `SpanStatisticsResult`, `OperationStatResult`
```

**README.md** — Make these specific changes:

1. **Line 11 (intro paragraph)**: Update the description to include trace analysis:
   - Change: "Give Claude (or any MCP-capable agent) read access to your trace data — search traces, inspect spans, map service dependencies — without leaving the conversation."
   - To: "Give Claude (or any MCP-capable agent) read access to your trace data — search traces, inspect spans, compare traces, compute span statistics, map service dependencies — without leaving the conversation."

2. **Line 19 (read-only bullet)**: Update tool count from 5 to 7:
   - Change: "Is **read-only**: all 5 tools carry `readOnlyHint: true`"
   - To: "Is **read-only**: all 7 tools carry `readOnlyHint: true`"

3. **Tool table (lines 25-31)**: Add two new rows to the end of the table:
   ```
   | `jaeger_compare_traces` | `GET /api/traces/{traceID}` ×2 | Structural diff between two traces |
   | `jaeger_span_statistics` | `GET /api/traces` | Per-operation latency and error stats |
   ```

4. **After the `jaeger_get_dependencies` section (after line 148)**: Add two new tool usage guide sections:

   ```markdown
   ### `jaeger_compare_traces`

   Structural diff between two traces. Accepts two `trace_id` hex strings and matches spans by `(operationName, serviceName, parentOperation)` — not span ID. Reports:

   - **Added spans** — present in trace B but not trace A
   - **Removed spans** — present in trace A but not trace B
   - **Changed spans** — matched but differ in duration or tags (shows deltas)
   - **Unchanged count** — number of identical spans

   Use to compare a slow trace against a fast one, or to see what changed between deployments.

   ### `jaeger_span_statistics`

   Per-operation latency percentiles and error rates. Fetches up to `limit` traces (default 20, max 100) for a service and aggregates all spans by operation name. Reports per operation:

   - `count` — total spans observed
   - `p50_duration_us`, `p95_duration_us`, `p99_duration_us` — latency percentiles
   - `error_count`, `error_rate` — errors (identified by `tags["error"] = "true"`)

   Use to find the slowest or most error-prone operations in a service.
   ```

5. **Library facade section (line 166)**: Update available methods line:
   - Change: "Available methods: `get_trace()`, `search_traces()`, `list_services()`, `get_dependencies()`."
   - To: "Available methods: `get_trace()`, `search_traces()`, `list_services()`, `get_dependencies()`, `compare_traces()`, `span_statistics()`."

6. **Domain objects line (line 168)**: Update to include new types:
   - Change: "Domain objects: `Span`, `Trace`, `TraceSummary`, `ServiceDep` — all with typed fields."
   - To: "Domain objects: `Span`, `Trace`, `TraceSummary`, `ServiceDep`, `TraceComparison`, `SpanIdentity`, `SpanChange`, `SpanStatisticsResult`, `OperationStatResult` — all with typed fields."

7. **Example queries section (lines 103-111)**: Add trace analysis examples:
   ```
   - "Compare trace `abc123` against trace `def456` — what spans changed?"
   - "What are the p95 latencies per operation in `order-service`?"
   ```
  </action>
  <verify>
    <automated>grep -c 'jaeger_compare_traces' README.md | xargs test 0 -lt && grep -c 'jaeger_span_statistics' README.md | xargs test 0 -lt && grep -c '\[0.3.0\]' CHANGELOG.md | xargs test 0 -lt && grep -c 'all 7 tools' README.md | xargs test 0 -lt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- CHANGELOG.md has `## [0.3.0] — 2026-06-16` section above v0.2.0
- CHANGELOG documents both jaeger_compare_traces and jaeger_span_statistics tools
- CHANGELOG documents both facade methods (compare_traces, span_statistics)
- README tool table has 7 rows (5 original + 2 new)
- README has usage guide sections for both new tools
- README intro mentions "compare traces" and "span statistics"
- README read-only line says "7 tools"
- README facade section lists compare_traces() and span_statistics()
- README domain objects line includes TraceComparison, SpanStatisticsResult, etc.
- README example queries include trace analysis examples
  </acceptance_criteria>
  <done>CHANGELOG has v0.3.0 section with both tools documented; README shows 7 tools with trace analysis sections and usage examples</done>
</task>

<task type="auto">
  <name>Task 3: Test suite verification</name>
  <files></files>
  <read_first></read_first>
  <action>
Run the full test suite with coverage to confirm nothing is broken by the documentation changes:

```bash
python3 -m pytest tests/ -v --cov=src/jaeger_mcp --cov-report=term-missing --cov-fail-under=90
```

Also run the linter to ensure no formatting issues in modified files:

```bash
python3 -m ruff check src tests
python3 -m ruff format --check src tests
```

If any tests fail or coverage drops below 90%, investigate and fix. The documentation-only changes in this plan should NOT affect tests — any failure indicates a pre-existing issue that must be resolved.

Verify the version is importable:

```bash
python3 -c "from jaeger_mcp import __version__; assert __version__ == '0.3.0', f'Got {__version__}'"
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --cov=src/jaeger_mcp --cov-fail-under=90 -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- All 237+ tests pass (zero failures)
- Coverage is >= 90% (current baseline: 97.64%)
- Ruff check reports no errors
- `from jaeger_mcp import __version__` returns "0.3.0"
  </acceptance_criteria>
  <done>Full test suite passes, coverage >= 90%, linter clean, version importable as 0.3.0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this phase modifies documentation and version metadata only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | pyproject.toml version | accept | Version is verified by test assertion (`__version__ == '0.3.0'`); no runtime security impact |
| T-10-02 | Information Disclosure | CHANGELOG.md / README.md | accept | All content is public documentation; no secrets or internal details exposed |
</threat_model>

<verification>
1. `grep '"0.3.0"' pyproject.toml src/jaeger_mcp/__init__.py` — both files show 0.3.0
2. `grep -c 'jaeger_compare_traces' README.md` — at least 3 mentions (table + usage guide + example)
3. `grep -c 'jaeger_span_statistics' README.md` — at least 3 mentions (table + usage guide + example)
4. `grep 'all 7 tools' README.md` — tool count updated
5. `grep '\[0.3.0\]' CHANGELOG.md` — changelog section exists
6. `python3 -m pytest tests/ --cov=src/jaeger_mcp --cov-fail-under=90 -q` — all tests pass
</verification>

<success_criteria>
- pyproject.toml version is "0.3.0"
- __init__.py __version__ is "0.3.0"
- CHANGELOG has v0.3.0 section documenting both new tools and facade methods
- README shows 7 tools in table, has trace analysis usage guide sections
- All tests pass, coverage >= 90%
- Linter clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-v030/10-01-SUMMARY.md`
</output>
